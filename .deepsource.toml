version = "1"

test_patterns = [
  "**/__tests__/**",
  "**/*.test.*",
  "**/*.spec.*"
]

exclude_patterns = [
  "dist/",
  "node_modules/",
  "coverage/",
  ".mimocode/"
]

[[analyzers]]
name = "javascript-typescript"
enabled = true

  [analyzers.meta]
  cyclomatic_complexity_threshold = "critical"
  max_functions = "50"
  skip_doc_coverage = ["test"]
  plugins = ["react"]
  environment = ["nodejs", "browser"]
  module_system = "es-modules"
  dialect = "typescript"

  [analyzers.meta.ratings]
    documentation_coverage = "A"

  [analyzers.meta.checks]
  # Disable Qwik-specific rules (project doesn't use Qwik)
  Biome_lint_correctness_useQwikValidLexicalScope = "off"
  # JS-0067 is a false positive for ES module exports — top-level declarations
  # in ES modules are module-scoped, not global. DeepSource docs confirm this.
  JS_0067 = "off"
